<!--pages/audioList/audioList.wxml-->
<view class="body">
    <block wx:for="{{audioData}}">
        <view class="every" data-listid="{{index}}" bindtap="jumpDetail">
            <image class="bacimg" src="https://www.niushaoda.club/thinkphp-3.2/images/type/listen.png" />
            <text class="title">{{item.title}}</text>
        </view>
    </block>
</view>
